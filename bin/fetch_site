#!/usr/bin/env ruby
require_relative "../lib/url_fetcher"

urls = ARGV

if urls.empty?
  puts "No urls supplied" and return
end

urls.each do |url|
  result = UrlFetcher.new(url:).process
  puts result
end
